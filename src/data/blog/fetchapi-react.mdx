---
title: "Fetch API Di React JS"
publishedAt: "2022-11-28"
author: "RakaTrust"
summary: "Cara Mengkonsumsi Api Di React JS"
tags: ["react", "javascript", "api", "tutorial"]
---

<div align="center">
  <img
    src="/react2.png"
    alt="alt of the image"
    width="500"
    height="500"
    title="title of the image"
  />
</div>

Selamat Sore, Kali ini kita langsung belajar cara mengambil dan menampilkan data API. Seperti yang kalian ketahui bahwa react js sebagai Frontend hanya perlu API untuk dikelola, salah satunya adalah dengan menampilkan data. Oke untuk saat ini kita coba untuk mengambil dan menampilkan data dari API publik dari github.

Lalu pada pada file App.js ketikkan kode seperti berikut ini.

```jsx:App.js
import React, { useState, useEffect } from 'react'
import './App.css';

function App() {
  const url = 'https://api.github.com/users'

  const [users, setUsers] = useState([])

  const getDataUsers = async () => {
    const response = await fetch('https://api.github.com/users')
    const dataku = await response.json()
    const users = dataku.slice(0, 8)
    setUsers(users)
  }

  useEffect(() => {
    getDataUsers()
  }, [])

  return (
    <div className="App">
      <h3>List Users</h3>
      {users.map((user) => {
        return (
          <p key={user.id}>
            {user.login}
          </p>
        )
      })}
    </div>
  );
}

export default App;
```

Jika sudah silahkan save dan cek hasilnya dengan url localhost:3000

#Penjelasan :

- Pertama kita buat variable dengan nama url untuk menyimpan alamat API yang ingin kita ambil datanya
- Kemudian kita buat variable users dan setUsers untuk menyimpan data yang dibawa oleh useState
- Lalu kita fetch datanya secara asynchronous seperti kode di atas. Dan kita simpan data tersebut menggunakan fungsi setUsers(users)
- Oh ya disitu ada fungsi slice, karena kita hanya ingin menampilkan beberapa data saja
- Setelah itu, kita jalankan fungsi getDataUsers() menggunakan useEffect agar lebih simple
- Terakhir, untuk menampilkan ke view, kita gunakan fungsi map()

Gimana? mudah? wkwk kalo masih ngga ngerti nanti saya cari best practice untuk fetch data di react js,
Semoga bermanfaat tips kali ini thank you semua selamat sore...
